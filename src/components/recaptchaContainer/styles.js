import { makeStyles } from '@material-ui/core/styles';

const recaptchaContainerStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'center',
    paddingBottom: '2rem',
    paddingTop: '1.5rem',
  },
});

export default recaptchaContainerStyles;
