import { makeStyles } from '@material-ui/core';

export const UseStylesTooltip = makeStyles({
  arrow: {
    color: '#404040',
  },
  tooltip: {
    backgroundColor: '#404040',
    boxShadow: '0px 1px 10px rgba(0, 0, 0, 0.1)',
    borderRadius: '2px',
    padding: '6px 12px 4px',
    fontSize: '12px',
    lineHeight: '16px',
    fontFamily: 'Poppins',
    fontWeight: '400',
  },
});

export default UseStylesTooltip;
